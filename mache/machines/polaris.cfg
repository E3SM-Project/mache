# Options related to deploying an e3sm-unified conda environment on supported
# machines
[e3sm_unified]

# the unix group for permissions for the e3sm-unified conda environment
group = E3SMinput

# the path to the directory where activation scripts, the base environment, and
# system libraries will be deployed
base_path = /lus/grand/projects/E3SMinput/soft/e3sm-unified


# config options related to data needed by diagnostics software such as
# e3sm_diags and MPAS-Analysis
[diagnostics]

# The base path to the diagnostics directory
base_path = /lus/grand/projects/E3SMinput/diagnostics

# the unix group for permissions for diagnostics
group = E3SMinput


# The parallel section describes options related to running jobs in parallel
[parallel]

# parallel system of execution
system = pbs

# account for running diagnostics jobs
account = E3SMinput

# available partition(s) (default is the first)
partitions = prod

# whether to use mpirun or srun to run a task
parallel_executable = mpiexec --label

# the flag to `parallel_executable` to specify the number of cpus per task
cpus_per_task_flag = --depth

# Leave unset on Polaris: Cray MPI docs indicate GPU mapping is handled with
# CUDA_VISIBLE_DEVICES/affinity scripts, not a numeric "gpus per task" flag.
gpus_per_task_flag =

# gpus per node on the machine
gpus_per_node = 4

# cpu cores per node (without hyperthreading)
cores_per_node = 32

# the maximum number of MPI tasks per node
max_mpi_tasks_per_node = 4

# CPU binding (cores or threads)
cpu_bind = cores


# parallel options for gnu compiler
[parallel.gnu]

# no gpus
gpus_per_node = 0

# the maximum number of MPI tasks per node
max_mpi_tasks_per_node = 32


# config options related to synchronizing files
[sync]

# the full hostname of the machine
hostname = polaris.alcf.anl.gov


# Options related to machine discovery
[discovery]

# a regular expression used to identify this machine from its hostname
hostname_re = ^polaris
